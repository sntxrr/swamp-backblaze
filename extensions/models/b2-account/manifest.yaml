manifestVersion: 1

name: "@sntxrr/b2-account"
version: "2026.08.05.1"
description: "Inventory a Backblaze B2 account — one read-only scan method emits a resource per bucket and per application key, plus a summary, via the B2 Native API v4."
repository: "https://github.com/sntxrr/swamp-backblaze"

paths:
  base: manifest

models:
  - b2_account.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - backblaze
  - b2
  - storage
  - account
  - inventory
  - audit

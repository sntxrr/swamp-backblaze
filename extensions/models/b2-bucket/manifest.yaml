manifestVersion: 1

name: "@sntxrr/b2-bucket"
version: "2026.08.05.1"
description: "Manage one Backblaze B2 bucket via the B2 Native API v4 — sync, create, update, idempotent delete, and get/set event notification rules — with first-class lifecycle rules so restic-pruned hidden file versions are actually deleted."
repository: "https://github.com/sntxrr/swamp-backblaze"

paths:
  base: manifest

models:
  - b2_bucket.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - backblaze
  - b2
  - storage
  - bucket
  - lifecycle
  - restic
